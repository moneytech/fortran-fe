# Configure looks for the existence of this file to auto-config each language.
# We define several parameters used by configure:
#
# language	- name of language as it would appear in $(LANGUAGES)
# compilers	- value to add to $(COMPILERS)
# stagestuff	- files to add to $(STAGESTUFF)
# diff_excludes	- files to ignore when building diffs between two versions.

language="f95"

compilers="f951\$(exeext)"

stagestuff=

gtfiles="\$(srcdir)/f95/f95-lang.c \$(srcdir)/f95/trans-const.c \$(srcdir)/f95/trans-decl.c \$(srcdir)/f95/trans-intrinsic.c \$(srcdir)/f95/trans-io.c \$(srcdir)/f95/trans-types.h \$(srcdir)/f95/trans.h"

#outputs=g95/Makefile

